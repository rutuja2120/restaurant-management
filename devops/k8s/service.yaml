apiVersion: v1
kind: Service
metadata:
  name: restaurant-system-service
spec:
  type: LoadBalancer
  selector:
    app: restaurant-system
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
